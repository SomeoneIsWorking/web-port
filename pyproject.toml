[project]
name = "web-port"
version = "0.1.0"
description = "Deterministic browser build dependencies for native ports"
requires-python = ">=3.11"
dependencies = ["cmake==4.1.0", "ninja==1.13.0", "pytest==9.0.2", "ruff==0.12.10"]

[tool.uv]
package = false
